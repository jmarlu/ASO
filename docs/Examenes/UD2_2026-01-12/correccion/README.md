# Correccion examen UD2 (2026-01-12)

Uso rapido:

```bash
./check_entrega.sh /ruta/al/examen_ud2_entrega.tar.gz
```

El script valida la estructura y comprueba evidencias basicas de cada parte.
Devuelve puntuacion sobre 10.

Si necesitas reglas mas estrictas, edita las expresiones de `rg` dentro de
`check_entrega.sh`.
